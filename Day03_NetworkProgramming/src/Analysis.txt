服务器客户端的工作流程
UDP(无连接,不可靠传输(丢包概率大),面向数据报,全双工)
->客户端发送请求(send(request))
->服务器接受请求并解析receive(request)
->服务器响应请求并返回给客户端(send(response));
->客户端接收服务器返回的请求(receive(response))

TCP(有连接,可靠传输,面向字节流,全双工)
*连接:网络连接,通信双方互相保存了对方的核心关键信息(IP/端口);
*断开连接:删除双方的关键信息